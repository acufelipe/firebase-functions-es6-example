{
  "name": "firebase-functions-es6-example",
  "version": "1.0.0",
  "description": "Example of ES6 syntax in Firebase Cloud Functions",
  "license": "MIT",
  "author": "Aleksey Razbakov",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src/ -d dist",
    "serve": "yarn build && firebase serve --only functions",
    "deploy": "yarn build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "~6.0.0",
    "firebase-functions": "^2.0.5"
  },
  "devDependencies": {
    "babel-runtime": "^6",
    "babel-cli": "latest",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0"
  },
  "engines": {
    "node": "8"
  }
}